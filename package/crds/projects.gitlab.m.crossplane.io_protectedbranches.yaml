---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: protectedbranches.projects.gitlab.m.crossplane.io
spec:
  group: projects.gitlab.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - gitlab
    kind: ProtectedBranch
    listKind: ProtectedBranchList
    plural: protectedbranches
    singular: protectedbranch
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    - jsonPath: .spec.forProvider.branchName
      name: BRANCH
      type: string
    - jsonPath: .spec.forProvider.projectId
      name: PROJECT
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: A ProtectedBranch is a managed resource that represents a GitLab
          Protected Branch
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: A ProtectedBranchSpec defines the desired state of a GitLab
              Protected Branch.
            properties:
              forProvider:
                description: ProtectedBranchParameters defines the desired state of
                  a GitLab Protected Branch.
                properties:
                  allowForcePush:
                    description: AllowForcePush allows force push to the protected
                      branch.
                    type: boolean
                  branchName:
                    description: BranchName is the name of the branch to protect.
                    type: string
                  codeOwnerApprovalRequired:
                    description: CodeOwnerApprovalRequired requires code owner approval
                      for merge requests.
                    type: boolean
                  mergeAccessLevels:
                    description: MergeAccessLevels represents the merge access levels
                      for the protected branch.
                    items:
                      description: BranchAccessDescription represents the access control
                        for a protected branch.
                      properties:
                        accessLevel:
                          description: AccessLevel represents the access level for
                            the branch.
                          type: integer
                        accessLevelDescription:
                          description: AccessLevelDescription is the description of
                            the access level.
                          type: string
                        groupId:
                          description: GroupID is the ID of the group with access.
                          format: int64
                          type: integer
                        userId:
                          description: UserID is the ID of the user with access.
                          format: int64
                          type: integer
                      type: object
                    type: array
                  projectId:
                    description: ProjectID is the ID of the project.
                    type: string
                  projectIdRef:
                    description: ProjectIDRef is a reference to a project to retrieve
                      its projectId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  projectIdSelector:
                    description: ProjectIDSelector selects reference to a project
                      to retrieve its projectId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  pushAccessLevels:
                    description: PushAccessLevels represents the push access levels
                      for the protected branch.
                    items:
                      description: BranchAccessDescription represents the access control
                        for a protected branch.
                      properties:
                        accessLevel:
                          description: AccessLevel represents the access level for
                            the branch.
                          type: integer
                        accessLevelDescription:
                          description: AccessLevelDescription is the description of
                            the access level.
                          type: string
                        groupId:
                          description: GroupID is the ID of the group with access.
                          format: int64
                          type: integer
                        userId:
                          description: UserID is the ID of the user with access.
                          format: int64
                          type: integer
                      type: object
                    type: array
                  unprotectAccessLevels:
                    description: UnprotectAccessLevels represents the unprotect access
                      levels for the protected branch.
                    items:
                      description: BranchAccessDescription represents the access control
                        for a protected branch.
                      properties:
                        accessLevel:
                          description: AccessLevel represents the access level for
                            the branch.
                          type: integer
                        accessLevelDescription:
                          description: AccessLevelDescription is the description of
                            the access level.
                          type: string
                        groupId:
                          description: GroupID is the ID of the group with access.
                          format: int64
                          type: integer
                        userId:
                          description: UserID is the ID of the user with access.
                          format: int64
                          type: integer
                      type: object
                    type: array
                required:
                - branchName
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            description: A ProtectedBranchStatus represents the observed state of
              a GitLab Protected Branch.
            properties:
              atProvider:
                description: ProtectedBranchObservation represents the observed state
                  of a GitLab Protected Branch.
                properties:
                  allowForcePush:
                    description: AllowForcePush indicates if force push is allowed
                      to the protected branch.
                    type: boolean
                  codeOwnerApprovalRequired:
                    description: CodeOwnerApprovalRequired indicates if code owner
                      approval is required for merge requests.
                    type: boolean
                  id:
                    description: ID is the ID of the protected branch.
                    format: int64
                    type: integer
                  mergeAccessLevels:
                    description: MergeAccessLevels represents the merge access levels
                      for the protected branch.
                    items:
                      description: BranchAccessDescription represents the access control
                        for a protected branch.
                      properties:
                        accessLevel:
                          description: AccessLevel represents the access level for
                            the branch.
                          type: integer
                        accessLevelDescription:
                          description: AccessLevelDescription is the description of
                            the access level.
                          type: string
                        groupId:
                          description: GroupID is the ID of the group with access.
                          format: int64
                          type: integer
                        userId:
                          description: UserID is the ID of the user with access.
                          format: int64
                          type: integer
                      type: object
                    type: array
                  pushAccessLevels:
                    description: PushAccessLevels represents the push access levels
                      for the protected branch.
                    items:
                      description: BranchAccessDescription represents the access control
                        for a protected branch.
                      properties:
                        accessLevel:
                          description: AccessLevel represents the access level for
                            the branch.
                          type: integer
                        accessLevelDescription:
                          description: AccessLevelDescription is the description of
                            the access level.
                          type: string
                        groupId:
                          description: GroupID is the ID of the group with access.
                          format: int64
                          type: integer
                        userId:
                          description: UserID is the ID of the user with access.
                          format: int64
                          type: integer
                      type: object
                    type: array
                  unprotectAccessLevels:
                    description: UnprotectAccessLevels represents the unprotect access
                      levels for the protected branch.
                    items:
                      description: BranchAccessDescription represents the access control
                        for a protected branch.
                      properties:
                        accessLevel:
                          description: AccessLevel represents the access level for
                            the branch.
                          type: integer
                        accessLevelDescription:
                          description: AccessLevelDescription is the description of
                            the access level.
                          type: string
                        groupId:
                          description: GroupID is the ID of the group with access.
                          format: int64
                          type: integer
                        userId:
                          description: UserID is the ID of the user with access.
                          format: int64
                          type: integer
                      type: object
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
