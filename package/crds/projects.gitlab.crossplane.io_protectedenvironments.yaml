---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: protectedenvironments.projects.gitlab.crossplane.io
spec:
  group: projects.gitlab.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - gitlab
    kind: ProtectedEnvironment
    listKind: ProtectedEnvironmentList
    plural: protectedenvironments
    singular: protectedenvironment
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    - jsonPath: .spec.forProvider.name
      name: NAME
      type: string
    - jsonPath: .spec.forProvider.projectId
      name: PROJECT
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ProtectedEnvironment is a managed resource that represents a
          GitLab Protected Environment.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                description: ProtectedEnvironmentParameters define the desired state
                  of a GitLab Protected Environment.
                properties:
                  approvalRules:
                    description: ApprovalRules configures who can approve and how
                      many approvals are required.
                    items:
                      description: EnvironmentApprovalRuleParameters defines who can
                        approve and how many approvals are required.
                      properties:
                        accessLevel:
                          description: AccessLevel is a numeric GitLab access level
                            (30=Developer, 40=Maintainer, 60=Admin).
                          type: integer
                        groupId:
                          description: GroupID is a GitLab group ID allowed to approve.
                          format: int64
                          type: integer
                        groupInheritanceType:
                          description: |-
                            GroupInheritanceType controls how inherited group memberships are treated.
                            0 => direct only, 1 => include inherited.
                          format: int64
                          type: integer
                        requiredApprovals:
                          description: RequiredApprovals required for this rule.
                          format: int64
                          type: integer
                        userId:
                          description: UserID is a GitLab user ID allowed to approve.
                          format: int64
                          type: integer
                      type: object
                    type: array
                  deployAccessLevels:
                    description: DeployAccessLevels configures who can deploy to this
                      environment.
                    items:
                      description: EnvironmentAccessLevelParameters configures who
                        can deploy to the environment.
                      properties:
                        accessLevel:
                          description: AccessLevel is a numeric GitLab access level
                            (30=Developer, 40=Maintainer, 60=Admin).
                          type: integer
                        groupId:
                          description: GroupID is a GitLab group ID allowed to deploy.
                          format: int64
                          type: integer
                        groupInheritanceType:
                          description: |-
                            GroupInheritanceType controls how inherited group memberships are treated.
                            0 => direct only, 1 => include inherited.
                          format: int64
                          type: integer
                        userId:
                          description: UserID is a GitLab user ID allowed to deploy.
                          format: int64
                          type: integer
                      type: object
                    type: array
                  name:
                    description: Name is the environment name (e.g. "production",
                      "staging").
                    type: string
                  projectId:
                    description: ProjectID is the ID or path of the GitLab project.
                    type: string
                  projectIdRef:
                    description: ProjectIDRef is a reference to a Project to populate
                      projectId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  projectIdSelector:
                    description: ProjectIDSelector selects a reference to a Project
                      to populate projectId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  requiredApprovalCount:
                    description: RequiredApprovalCount is the unified (legacy) approval
                      count.
                    format: int64
                    type: integer
                required:
                - name
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
          status:
            properties:
              atProvider:
                description: ProtectedEnvironmentObservation reflects the observed
                  state from GitLab.
                properties:
                  approvalRules:
                    items:
                      description: EnvironmentApprovalRuleObservation reflects an
                        approval rule entry returned by GitLab.
                      properties:
                        accessLevel:
                          type: integer
                        accessLevelDescription:
                          type: string
                        groupId:
                          format: int64
                          type: integer
                        groupInheritanceType:
                          format: int64
                          type: integer
                        id:
                          format: int64
                          type: integer
                        requiredApprovals:
                          format: int64
                          type: integer
                        userId:
                          format: int64
                          type: integer
                      type: object
                    type: array
                  deployAccessLevels:
                    items:
                      description: EnvironmentAccessLevelObservation reflects a deploy
                        access level entry returned by GitLab.
                      properties:
                        accessLevel:
                          type: integer
                        accessLevelDescription:
                          type: string
                        groupId:
                          format: int64
                          type: integer
                        groupInheritanceType:
                          format: int64
                          type: integer
                        id:
                          format: int64
                          type: integer
                        userId:
                          format: int64
                          type: integer
                      type: object
                    type: array
                  name:
                    type: string
                  requiredApprovalCount:
                    format: int64
                    type: integer
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
