---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: appearances.instance.gitlab.m.crossplane.io
spec:
  group: instance.gitlab.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - gitlab
    kind: Appearance
    listKind: AppearanceList
    plural: appearances
    singular: appearance
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: A Appearance is a managed resource that represents a Gitlab instance
          Settings.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: A AppearanceSpec defines the desired state of a Gitlab Instance
              Settings.
            properties:
              forProvider:
                description: Defines the desired state of the Appearance.
                properties:
                  description:
                    description: Markdown text shown on the sign in / sign up page
                    type: string
                  email_header_and_footer_enabled:
                    description: Add header and footer to all outgoing emails if enabled.
                    type: boolean
                  favicon:
                    description: Instance favicon in .ico or .png format.
                    type: string
                  footer_message:
                    description: Message in the system footer bar.
                    type: string
                  header_logo:
                    description: Instance image used for the main navigation bar.
                    type: string
                  header_message:
                    description: Message in the system header bar.
                    type: string
                  logo:
                    description: Instance image used on the sign in / sign up page.
                    type: string
                  member_guidelines:
                    description: Markdown text shown on the group or project member
                      page for users with permission to change members.
                    type: string
                  message_background_color:
                    description: Background color for the system header / footer bar.
                    type: string
                  message_font_color:
                    description: Font color for the system header / footer bar.
                    type: string
                  new_project_guidelines:
                    description: Markdown text shown on the new project page.
                    type: string
                  profile_image_guidelines:
                    description: Markdown text shown on the profile page below Public
                      Avatar.
                    type: string
                  pwa_description:
                    description: An explanation of what the Progressive Web App does.
                      Used for the attribute description in manifest.json.
                    type: string
                  pwa_icon:
                    description: Icon used for Progressive Web App.
                    type: string
                  pwa_name:
                    description: Full name of the Progressive Web App. Used for the
                      attribute name in manifest.json.
                    type: string
                  pwa_short_name:
                    description: Short name for Progressive Web App.
                    type: string
                  title:
                    description: Instance title on the sign in / sign up page
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            description: A AppearanceStatus represents the observed state of the GitLab
              Instance Settings.
            properties:
              atProvider:
                description: Represents the observed state of the Appearance.
                properties:
                  description:
                    description: Markdown text shown on the sign in / sign up page
                    type: string
                  email_header_and_footer_enabled:
                    description: Add header and footer to all outgoing emails if enabled.
                    type: boolean
                  favicon:
                    description: Instance favicon in .ico or .png format.
                    type: string
                  footer_message:
                    description: Message in the system footer bar.
                    type: string
                  header_logo:
                    description: Instance image used for the main navigation bar.
                    type: string
                  header_message:
                    description: Message in the system header bar.
                    type: string
                  logo:
                    description: Instance image used on the sign in / sign up page.
                    type: string
                  member_guidelines:
                    description: Markdown text shown on the group or project member
                      page for users with permission to change members.
                    type: string
                  message_background_color:
                    description: Background color for the system header / footer bar.
                    type: string
                  message_font_color:
                    description: Font color for the system header / footer bar.
                    type: string
                  new_project_guidelines:
                    description: Markdown text shown on the new project page.
                    type: string
                  profile_image_guidelines:
                    description: Markdown text shown on the profile page below Public
                      Avatar.
                    type: string
                  pwa_description:
                    description: An explanation of what the Progressive Web App does.
                      Used for the attribute description in manifest.json.
                    type: string
                  pwa_icon:
                    description: Icon used for Progressive Web App.
                    type: string
                  pwa_name:
                    description: Full name of the Progressive Web App. Used for the
                      attribute name in manifest.json.
                    type: string
                  pwa_short_name:
                    description: Short name for Progressive Web App.
                    type: string
                  title:
                    description: Instance title on the sign in / sign up page
                    type: string
                required:
                - description
                - email_header_and_footer_enabled
                - favicon
                - footer_message
                - header_logo
                - header_message
                - logo
                - member_guidelines
                - message_background_color
                - message_font_color
                - new_project_guidelines
                - profile_image_guidelines
                - pwa_description
                - pwa_icon
                - pwa_name
                - pwa_short_name
                - title
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
