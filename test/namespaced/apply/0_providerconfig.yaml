apiVersion: gitlab.m.crossplane.io/v1beta1
kind: ProviderConfig
metadata:
  name: default
spec:
  baseURL: ($values.baseUrl)
  credentials:
    method: AccessToken
    secretRef:
      key: token
      name: gitlab-credentials
      namespace: default
    source: "Secret"
