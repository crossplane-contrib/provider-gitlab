/*
Copyright 2020 The Crossplane Authors.

Licensed under the Apache License, Version 2.0 (the "License");
you may not use this file except in compliance with the License.
You may obtain a copy of the License at

    http://www.apache.org/licenses/LICENSE-2.0

Unless required by applicable law or agreed to in writing, software
distributed under the License is distributed on an "AS IS" BASIS,
WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
See the License for the specific language governing permissions and
limitations under the License.
*/

// Code generated by hack/generate-cluster-scope.go - DO NOT EDIT.

package serviceaccountaccesstokens

import (
	"context"
	"net/http"
	"strconv"
	"strings"

	"github.com/crossplane/crossplane-runtime/v2/pkg/controller"
	"github.com/crossplane/crossplane-runtime/v2/pkg/event"
	"github.com/crossplane/crossplane-runtime/v2/pkg/feature"
	"github.com/crossplane/crossplane-runtime/v2/pkg/meta"
	"github.com/crossplane/crossplane-runtime/v2/pkg/reconciler/managed"
	"github.com/crossplane/crossplane-runtime/v2/pkg/resource"
	"github.com/crossplane/crossplane-runtime/v2/pkg/statemetrics"
	v2 "github.com/crossplane/crossplane/apis/v2/core/v2"
	"github.com/pkg/errors"
	gitlab "gitlab.com/gitlab-org/api/client-go/v2"
	corev1 "k8s.io/api/core/v1"
	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
	ctrl "sigs.k8s.io/controller-runtime"
	"sigs.k8s.io/controller-runtime/pkg/client"

	"github.com/crossplane-contrib/provider-gitlab/apis/cluster/groups/v1alpha1"
	"github.com/crossplane-contrib/provider-gitlab/pkg/cluster/clients"
	"github.com/crossplane-contrib/provider-gitlab/pkg/cluster/clients/groups"
	"github.com/crossplane-contrib/provider-gitlab/pkg/common"
	auth "github.com/crossplane-contrib/provider-gitlab/pkg/common/auth"
)

const (
	errNotServiceAccountAccessToken = "managed resource is not a Gitlab service account access token custom resource"
	errExternalNameNotInt           = "custom resource external name is not an integer"
	errFailedParseID                = "cannot parse Access Token ID to int"
	errCreateFailed                 = "cannot create Gitlab service account access token"
	errRotateFailed                 = "cannot rotate Gitlab service account access token"
	errDeleteFailed                 = "cannot delete Gitlab service account access token"
	errAccessTokenNotFound          = "cannot find Gitlab service account access token"
	errMissingGroupID               = "missing Spec.ForProvider.GroupID"
	errMissingServiceAccountID      = "missing Spec.ForProvider.ServiceAccountID"
	errSelfInformFailed             = "cannot read the self service account access token; the token referenced by the ProviderConfig may be expired or revoked - reseed the credentials secret with a valid token"
	errSelfRotateFailed             = "cannot self-rotate Gitlab service account access token"
	errSelfServiceAccountMismatch   = "the token referenced by the ProviderConfig does not belong to spec.forProvider.serviceAccountId; refusing to manage it - check that the credentials secret and serviceAccountId are wired to the same service account"

	// connectionSecretKeyToken is the connection-secret key under which the
	// token value is published. Self-mode detection requires the ProviderConfig
	// to read this exact key.
	connectionSecretKeyToken = "token"
)

// Condition surfaced on the managed resource to indicate which rotation mode is
// in effect. It is informational (not a Crossplane system condition) and is
// re-asserted on every reconcile.
const (
	// TypeSelfManaged is the condition type reporting self-managed mode.
	TypeSelfManaged v2.ConditionType = "SelfManaged"

	// ReasonProviderConfigReferencesManagedToken is set when the ProviderConfig
	// authenticates with the very token this resource manages (self-mode).
	ReasonProviderConfigReferencesManagedToken v2.ConditionReason = "ProviderConfigReferencesManagedToken"
	// ReasonOwnerManaged is set when the token is managed via group-owner endpoints.
	ReasonOwnerManaged v2.ConditionReason = "OwnerManaged"
)

// selfManagedCondition returns the SelfManaged condition for the given mode.
func selfManagedCondition(self bool) v2.Condition {
	c := v2.Condition{Type: TypeSelfManaged, LastTransitionTime: metav1.Now()}
	if self {
		c.Status = corev1.ConditionTrue
		c.Reason = ReasonProviderConfigReferencesManagedToken
		c.Message = "rotating via the self endpoints; the ProviderConfig authenticates with the token managed by this resource"
		return c
	}
	c.Status = corev1.ConditionFalse
	c.Reason = ReasonOwnerManaged
	return c
}

// SetupServiceAccountAccessToken adds a controller that reconciles
// group ServiceAccountAccessTokens.
func SetupServiceAccountAccessToken(mgr ctrl.Manager, o controller.Options) error {
	name := managed.ControllerName("cluster." + v1alpha1.ServiceAccountAccessTokenGroupKind)

	reconcilerOpts := []managed.ReconcilerOption{
		managed.WithExternalConnector(&connector{kube: mgr.GetClient(), newGitlabClientFn: groups.NewServiceAccountAccessTokenClient}),
		managed.WithInitializers(),
		managed.WithPollInterval(o.PollInterval),
		managed.WithLogger(o.Logger.WithValues("controller", name)),
		managed.WithRecorder(event.NewAPIRecorder(mgr.GetEventRecorderFor(name))),
	}

	if o.Features.Enabled(feature.EnableBetaManagementPolicies) {
		reconcilerOpts = append(reconcilerOpts, managed.WithManagementPolicies())
	}

	r := managed.NewReconciler(mgr,
		resource.ManagedKind(v1alpha1.ServiceAccountAccessTokenGroupVersionKind),
		reconcilerOpts...)

	if err := mgr.Add(statemetrics.NewMRStateRecorder(
		mgr.GetClient(), o.Logger, o.MetricOptions.MRStateMetrics, &v1alpha1.ServiceAccountAccessTokenList{}, o.MetricOptions.PollStateMetricInterval)); err != nil {
		return err
	}

	return ctrl.NewControllerManagedBy(mgr).
		Named(name).
		WithOptions(o.ForControllerRuntime()).
		For(&v1alpha1.ServiceAccountAccessToken{}).
		Complete(r)
}

// SetupServiceAccountAccessTokenGated adds a controller with CRD gate support.
func SetupServiceAccountAccessTokenGated(mgr ctrl.Manager, o controller.Options) error {
	o.Gate.Register(func() {
		if err := SetupServiceAccountAccessToken(mgr, o); err != nil {
			mgr.GetLogger().Error(err, "unable to setup reconciler", "gvk", v1alpha1.ServiceAccountAccessTokenGroupVersionKind.String())
		}
	}, v1alpha1.ServiceAccountAccessTokenGroupVersionKind)
	return nil
}

type connector struct {
	kube              client.Client
	newGitlabClientFn func(cfg common.Config) groups.ServiceAccountAccessTokenClient
}

func (c *connector) Connect(ctx context.Context, mg resource.Managed) (managed.ExternalClient, error) {
	cr, ok := mg.(*v1alpha1.ServiceAccountAccessToken)
	if !ok {
		return nil, errors.New(errNotServiceAccountAccessToken)
	}
	cfg, err := common.GetConfig(ctx, c.kube, cr)
	if err != nil {
		return nil, err
	}
	return &external{kube: c.kube, client: c.newGitlabClientFn(*cfg), self: isSelfManaged(cfg, cr)}, nil
}

// isSelfManaged reports whether the ProviderConfig this resource uses
// authenticates with the very token this resource writes to its connection
// secret. In that case the provider is acting as the service account itself and
// can only use the self endpoints (self-inform / self-rotate / self-revoke).
//
// Detection requires a PersonalAccessToken credential whose secret reference
// matches the resource's writeConnectionSecretToRef (namespace + name) and the
// published token key.
func isSelfManaged(cfg *common.Config, cr *v1alpha1.ServiceAccountAccessToken) bool {
	if cfg == nil || cfg.AuthMethod != auth.PersonalAccessToken {
		return false
	}
	ref := cfg.CredentialsSecretRef
	if ref == nil {
		return false
	}
	w := cr.GetWriteConnectionSecretToReference()
	if w == nil {
		return false
	}
	return ref.Namespace == w.Namespace &&
		ref.Name == w.Name &&
		ref.Key == connectionSecretKeyToken
}

type external struct {
	kube   client.Client
	client groups.ServiceAccountAccessTokenClient
	// self indicates the ProviderConfig authenticates with the token this
	// resource manages, so only the self endpoints are usable.
	self bool
}

func (e *external) Observe(ctx context.Context, mg resource.Managed) (managed.ExternalObservation, error) {
	cr, ok := mg.(*v1alpha1.ServiceAccountAccessToken)
	if !ok {
		return managed.ExternalObservation{}, errors.New(errNotServiceAccountAccessToken)
	}

	cr.Status.SetConditions(selfManagedCondition(e.self))

	if e.self {
		return e.observeSelf(ctx, cr)
	}
	return e.observeOwner(ctx, cr)
}

// observeSelf observes the token via the self-inform endpoint. The managed
// token is whatever the ProviderConfig authenticates with, so the external name
// is auto-adopted from the response.
func (e *external) observeSelf(ctx context.Context, cr *v1alpha1.ServiceAccountAccessToken) (managed.ExternalObservation, error) {
	at, res, err := e.client.GetServiceAccountSelf(gitlab.WithContext(ctx))
	if err != nil {
		// During deletion the self-revoke has already run, so the self
		// endpoints reject the (now revoked) credential with 401/403. That is
		// the expected terminal state: report the resource gone so the
		// finalizer is removed instead of wedging the delete in a retry loop.
		if meta.WasDeleted(cr) && clients.IsResponseUnauthorized(res) {
			return managed.ExternalObservation{ResourceExists: false}, nil
		}
		// A dead self-credential is unrecoverable by the provider: surface a
		// clear error rather than masquerading as "does not exist" (which would
		// push the resource into a doomed self-rotate loop).
		return managed.ExternalObservation{}, errors.Wrap(err, errSelfInformFailed)
	}

	// Safety: in self-managed mode we adopt whatever token the ProviderConfig
	// authenticates with. Refuse to manage it when it does not belong to the
	// service account this resource targets - otherwise a miswired credentials
	// secret could make us rotate or revoke the wrong service account's token
	// while the reconcile still reports success.
	if cr.Spec.ForProvider.ServiceAccountID != nil && at.UserID != *cr.Spec.ForProvider.ServiceAccountID {
		return managed.ExternalObservation{}, errors.New(errSelfServiceAccountMismatch)
	}

	meta.SetExternalName(cr, strconv.FormatInt(at.ID, 10))
	cr.Status.AtProvider = groups.GenerateServiceAccountAccessTokenObservation(at)

	if cr.Spec.ForProvider.RenewalPeriodDays != nil {
		cr.Status.RenewAt = common.ComputeNextRotation(at.CreatedAt, at.ExpiresAt, cr.Spec.ForProvider.RenewBeforeDays)
	} else {
		cr.Status.RenewAt = nil
	}

	if groups.ShouldRotateServiceAccountAccessToken(&cr.Spec.ForProvider, at) {
		return managed.ExternalObservation{ResourceExists: false}, nil
	}

	cr.Status.SetConditions(v2.Available())

	return managed.ExternalObservation{
		ResourceExists:   true,
		ResourceUpToDate: true,
	}, nil
}

// observeOwner observes the token via the group-owner list endpoint.
func (e *external) observeOwner(ctx context.Context, cr *v1alpha1.ServiceAccountAccessToken) (managed.ExternalObservation, error) {
	externalName := meta.GetExternalName(cr)
	if externalName == "" {
		return managed.ExternalObservation{}, nil
	}

	accessTokenID, err := strconv.Atoi(externalName)
	if err != nil {
		return managed.ExternalObservation{}, errors.Wrap(err, errFailedParseID)
	}

	if cr.Spec.ForProvider.GroupID == nil {
		return managed.ExternalObservation{}, errors.New(errMissingGroupID)
	}

	if cr.Spec.ForProvider.ServiceAccountID == nil {
		return managed.ExternalObservation{}, errors.New(errMissingServiceAccountID)
	}

	at, res, err := groups.GetServiceAccountAccessToken(
		e.client,
		*cr.Spec.ForProvider.GroupID,
		*cr.Spec.ForProvider.ServiceAccountID,
		int64(accessTokenID),
		gitlab.WithContext(ctx),
	)
	if err != nil {
		if clients.IsResponseNotFound(res) {
			return managed.ExternalObservation{ResourceExists: false}, nil
		}
		return managed.ExternalObservation{}, errors.Wrap(err, errAccessTokenNotFound)
	}

	// The token no longer exists (e.g. revoked or deleted out-of-band). There is no
	// get-by-id endpoint for service account tokens, so a missing token surfaces as
	// a nil result rather than a 404.
	if at == nil {
		return managed.ExternalObservation{ResourceExists: false}, nil
	}

	cr.Status.AtProvider = groups.GenerateServiceAccountAccessTokenObservation(at)

	if cr.Spec.ForProvider.RenewalPeriodDays != nil {
		cr.Status.RenewAt = common.ComputeNextRotation(at.CreatedAt, at.ExpiresAt, cr.Spec.ForProvider.RenewBeforeDays)
	} else {
		cr.Status.RenewAt = nil
	}

	if groups.ShouldRotateServiceAccountAccessToken(&cr.Spec.ForProvider, at) {
		return managed.ExternalObservation{ResourceExists: false}, nil
	}

	cr.Status.SetConditions(v2.Available())

	return managed.ExternalObservation{
		ResourceExists:   true,
		ResourceUpToDate: true,
	}, nil
}

func (e *external) Create(ctx context.Context, mg resource.Managed) (managed.ExternalCreation, error) {
	cr, ok := mg.(*v1alpha1.ServiceAccountAccessToken)
	if !ok {
		return managed.ExternalCreation{}, errors.New(errNotServiceAccountAccessToken)
	}

	if e.self {
		return e.createSelf(ctx, cr)
	}
	return e.createOwner(ctx, cr)
}

// createSelf rotates the token via the self endpoint. It is reached only when
// Observe reported a rotation is due; a brand-new token cannot be bootstrapped
// in self-mode (the token must already exist - it is the credential).
func (e *external) createSelf(ctx context.Context, cr *v1alpha1.ServiceAccountAccessToken) (managed.ExternalCreation, error) {
	at, _, err := e.client.RotateServiceAccountSelf(
		groups.GenerateRotateServiceAccountSelfOptions(&cr.Spec.ForProvider),
		gitlab.WithContext(ctx),
	)
	if err != nil {
		return managed.ExternalCreation{}, errors.Wrap(err, errSelfRotateFailed)
	}

	meta.SetExternalName(cr, strconv.FormatInt(at.ID, 10))
	return managed.ExternalCreation{
		ConnectionDetails: managed.ConnectionDetails{connectionSecretKeyToken: []byte(at.Token)},
	}, nil
}

// createOwner creates or rotates the token via the group-owner endpoints.
func (e *external) createOwner(ctx context.Context, cr *v1alpha1.ServiceAccountAccessToken) (managed.ExternalCreation, error) {
	if cr.Spec.ForProvider.GroupID == nil {
		return managed.ExternalCreation{}, errors.New(errMissingGroupID)
	}

	if cr.Spec.ForProvider.ServiceAccountID == nil {
		return managed.ExternalCreation{}, errors.New(errMissingServiceAccountID)
	}

	// If a token ID is already set as the external name, try rotating it first.
	// Rotation atomically revokes the old token and issues a new one (with a new ID).
	if existingID, err := strconv.ParseInt(meta.GetExternalName(cr), 10, 64); err == nil && existingID > 0 {
		at, res, rotErr := e.client.RotateServiceAccountPersonalAccessToken(
			*cr.Spec.ForProvider.GroupID,
			*cr.Spec.ForProvider.ServiceAccountID,
			existingID,
			groups.GenerateRotateServiceAccountAccessTokenOptions(&cr.Spec.ForProvider),
			gitlab.WithContext(ctx),
		)
		if rotErr == nil {
			meta.SetExternalName(cr, strconv.FormatInt(at.ID, 10))
			return managed.ExternalCreation{
				ConnectionDetails: managed.ConnectionDetails{connectionSecretKeyToken: []byte(at.Token)},
			}, nil
		}
		// Only fall through to a fresh create when the existing token is truly
		// gone. Any other failure (transient error, 403, 5xx, transport error)
		// must surface rather than silently minting a second token.
		if !rotateFallsBackToCreate(res, rotErr) {
			return managed.ExternalCreation{}, errors.Wrap(rotErr, errRotateFailed)
		}
		// Token was already revoked or no longer exists; fall through to fresh create.
	}

	at, _, err := e.client.CreateServiceAccountPersonalAccessToken(
		*cr.Spec.ForProvider.GroupID,
		*cr.Spec.ForProvider.ServiceAccountID,
		groups.GenerateCreateServiceAccountAccessTokenOptions(&cr.Spec.ForProvider),
		gitlab.WithContext(ctx),
	)
	if err != nil {
		return managed.ExternalCreation{}, errors.Wrap(err, errCreateFailed)
	}

	meta.SetExternalName(cr, strconv.FormatInt(at.ID, 10))
	return managed.ExternalCreation{
		ConnectionDetails: managed.ConnectionDetails{connectionSecretKeyToken: []byte(at.Token)},
	}, nil
}

// rotateFallsBackToCreate reports whether a failed owner-mode rotation should
// fall through to creating a fresh token. This is only safe when the existing
// token is genuinely gone:
//
//   - 404: the token id is unknown (deleted out-of-band, or wrong service account)
//   - 400 "Token already revoked": the token exists but is revoked
//
// Any other error (transient, 403, 5xx, transport) must not trigger a create, to
// avoid minting a second token on failures unrelated to the token's existence.
func rotateFallsBackToCreate(res *gitlab.Response, err error) bool {
	if clients.IsResponseNotFound(res) {
		return true
	}
	if res != nil && res.StatusCode == http.StatusBadRequest &&
		err != nil && strings.Contains(strings.ToLower(err.Error()), "revoked") {
		return true
	}
	return false
}

// Update is currently a no-op as the only updatable field is expiration, which is handled by rotation in Observe.
func (e *external) Update(_ context.Context, _ resource.Managed) (managed.ExternalUpdate, error) {
	return managed.ExternalUpdate{}, nil
}

func (e *external) Delete(ctx context.Context, mg resource.Managed) (managed.ExternalDelete, error) {
	cr, ok := mg.(*v1alpha1.ServiceAccountAccessToken)
	if !ok {
		return managed.ExternalDelete{}, errors.New(errNotServiceAccountAccessToken)
	}

	if e.self {
		// Revoke the token the ProviderConfig authenticates with. This breaks
		// every resource using that ProviderConfig; use deletionPolicy: Orphan
		// to keep the token.
		res, err := e.client.RevokeServiceAccountSelf(gitlab.WithContext(ctx))
		if err != nil {
			// A second reconcile hitting an already-revoked token can no longer
			// authenticate and gets 401/403. Treat that as an idempotent
			// success so the delete does not wedge on an already-completed
			// revoke.
			if clients.IsResponseUnauthorized(res) {
				return managed.ExternalDelete{}, nil
			}
			return managed.ExternalDelete{}, errors.Wrap(err, errDeleteFailed)
		}
		return managed.ExternalDelete{}, nil
	}

	accessTokenID, err := strconv.Atoi(meta.GetExternalName(cr))
	if err != nil {
		return managed.ExternalDelete{}, errors.New(errExternalNameNotInt)
	}

	if cr.Spec.ForProvider.GroupID == nil {
		return managed.ExternalDelete{}, errors.New(errMissingGroupID)
	}

	if cr.Spec.ForProvider.ServiceAccountID == nil {
		return managed.ExternalDelete{}, errors.New(errMissingServiceAccountID)
	}

	_, err = e.client.RevokeServiceAccountPersonalAccessToken(
		*cr.Spec.ForProvider.GroupID,
		*cr.Spec.ForProvider.ServiceAccountID,
		int64(accessTokenID),
		gitlab.WithContext(ctx),
	)

	return managed.ExternalDelete{}, errors.Wrap(err, errDeleteFailed)
}

func (e *external) Disconnect(ctx context.Context) error {
	// Disconnect is not implemented as it is a new method required by the SDK
	return nil
}
