/*
Copyright 2021 The Crossplane Authors.

Licensed under the Apache License, Version 2.0 (the "License");
you may not use this file except in compliance with the License.
You may obtain a copy of the License at

    http://www.apache.org/licenses/LICENSE-2.0

Unless required by applicable law or agreed to in writing, software
distributed under the License is distributed on an "AS IS" BASIS,
WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
See the License for the specific language governing permissions and
limitations under the License.
*/

// Code generated by hack/generate-cluster-scope.go - DO NOT EDIT.

package applications

import (
	"context"
	"strconv"

	"github.com/crossplane/crossplane-runtime/v2/pkg/controller"
	"github.com/crossplane/crossplane-runtime/v2/pkg/errors"
	"github.com/crossplane/crossplane-runtime/v2/pkg/event"
	"github.com/crossplane/crossplane-runtime/v2/pkg/feature"
	"github.com/crossplane/crossplane-runtime/v2/pkg/meta"
	"github.com/crossplane/crossplane-runtime/v2/pkg/reconciler/managed"
	"github.com/crossplane/crossplane-runtime/v2/pkg/resource"
	"github.com/crossplane/crossplane-runtime/v2/pkg/statemetrics"
	v2 "github.com/crossplane/crossplane/apis/v2/core/v2"
	gitlab "gitlab.com/gitlab-org/api/client-go/v2"
	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
	ctrl "sigs.k8s.io/controller-runtime"
	"sigs.k8s.io/controller-runtime/pkg/client"

	"github.com/crossplane-contrib/provider-gitlab/apis/cluster/instance/v1alpha1"
	"github.com/crossplane-contrib/provider-gitlab/pkg/cluster/clients/instance"
	"github.com/crossplane-contrib/provider-gitlab/pkg/common"
)

const (
	errNotApplication  = "managed resource is not a GitLab Application custom resource"
	errGetFailed       = "cannot list GitLab Applications"
	errCreateFailed    = "cannot create GitLab Application"
	errDeleteFailed    = "cannot delete GitLab Application"
	errRenewFailed     = "cannot renew GitLab Application secret"
	errIDNotInt        = "external name is not a valid integer ID"
	errNoConnectionRef = "writeConnectionSecretToRef must be specified to receive the application secret"
	errCannotUpdate    = "GitLab Applications cannot be updated after creation. To change any fields, delete and recreate the resource."
)

// SetupApplication adds a controller that reconciles GitLab OAuth Applications.
func SetupApplication(mgr ctrl.Manager, o controller.Options) error {
	name := managed.ControllerName("cluster." + v1alpha1.ApplicationGroupKind)

	reconcilerOpts := []managed.ReconcilerOption{
		managed.WithExternalConnector(&connector{kube: mgr.GetClient(), newGitlabClientFn: instance.NewApplicationClient}),
		managed.WithInitializers(),
		managed.WithPollInterval(o.PollInterval),
		managed.WithLogger(o.Logger.WithValues("controller", name)),
		managed.WithRecorder(event.NewAPIRecorder(mgr.GetEventRecorderFor(name))),
	}

	if o.Features.Enabled(feature.EnableBetaManagementPolicies) {
		reconcilerOpts = append(reconcilerOpts, managed.WithManagementPolicies())
	}

	r := managed.NewReconciler(mgr,
		resource.ManagedKind(v1alpha1.ApplicationGroupVersionKind),
		reconcilerOpts...)

	if err := mgr.Add(statemetrics.NewMRStateRecorder(
		mgr.GetClient(), o.Logger, o.MetricOptions.MRStateMetrics, &v1alpha1.ApplicationList{}, o.MetricOptions.PollStateMetricInterval)); err != nil {
		return err
	}

	return ctrl.NewControllerManagedBy(mgr).
		Named(name).
		WithOptions(o.ForControllerRuntime()).
		For(&v1alpha1.Application{}).
		Complete(r)
}

// SetupApplicationGated adds a controller with CRD gate support.
func SetupApplicationGated(mgr ctrl.Manager, o controller.Options) error {
	o.Gate.Register(func() {
		if err := SetupApplication(mgr, o); err != nil {
			mgr.GetLogger().Error(err, "unable to setup reconciler", "gvk", v1alpha1.ApplicationGroupVersionKind.String())
		}
	}, v1alpha1.ApplicationGroupVersionKind)
	return nil
}

// connector is responsible for producing an ExternalClient for GitLab Applications.
type connector struct {
	kube              client.Client
	newGitlabClientFn func(cfg common.Config) instance.ApplicationClient
}

// Connect creates a new GitLab Applications client.
func (c *connector) Connect(ctx context.Context, mg resource.Managed) (managed.ExternalClient, error) {
	cr, ok := mg.(*v1alpha1.Application)
	if !ok {
		return nil, errors.New(errNotApplication)
	}
	cfg, err := common.GetConfig(ctx, c.kube, cr)
	if err != nil {
		return nil, err
	}
	return &external{client: c.newGitlabClientFn(*cfg)}, nil
}

// external is the external client for GitLab Applications.
type external struct {
	client instance.ApplicationClient
}

// Observe checks if the GitLab Application exists and whether it is up to date.
// Since the GitLab API has no Get-by-ID endpoint, it paginates through all
// applications to find the matching one by numeric ID.
func (e *external) Observe(ctx context.Context, mg resource.Managed) (managed.ExternalObservation, error) {
	cr, ok := mg.(*v1alpha1.Application)
	if !ok {
		return managed.ExternalObservation{}, errors.New(errNotApplication)
	}

	externalName := meta.GetExternalName(cr)
	if externalName == "" {
		return managed.ExternalObservation{ResourceExists: false}, nil
	}

	appID, err := strconv.ParseInt(externalName, 10, 64)
	if err != nil {
		return managed.ExternalObservation{}, errors.New(errIDNotInt)
	}

	app, err := e.findApplicationByIDPaginated(ctx, appID)
	if err != nil {
		return managed.ExternalObservation{}, err
	}
	if app == nil {
		return managed.ExternalObservation{ResourceExists: false}, nil
	}

	nextRenewalAt := cr.Status.AtProvider.NextRenewalAt
	cr.Status.AtProvider = instance.GenerateApplicationObservation(app)
	cr.Status.AtProvider.NextRenewalAt = nextRenewalAt
	cr.Status.SetConditions(v2.Available())

	upToDate := instance.IsApplicationUpToDate(&cr.Spec.ForProvider, app) &&
		!instance.IsApplicationRenewalDue(&cr.Spec.ForProvider, cr.Status.AtProvider.NextRenewalAt)

	return managed.ExternalObservation{
		ResourceExists:   true,
		ResourceUpToDate: upToDate,
	}, nil
}

// findApplicationByIDPaginated iterates through all pages of GitLab applications
// and returns the one matching the given numeric ID, or nil if not found.
func (e *external) findApplicationByIDPaginated(ctx context.Context, appID int64) (*gitlab.Application, error) {
	for page := int64(1); ; {
		apps, resp, err := e.client.ListApplications(&gitlab.ListApplicationsOptions{
			ListOptions: gitlab.ListOptions{
				Page:    page,
				PerPage: 100,
			},
		}, gitlab.WithContext(ctx))
		if err != nil {
			return nil, errors.Wrap(err, errGetFailed)
		}

		if app := instance.FindApplicationByID(apps, appID); app != nil {
			return app, nil
		}

		if resp == nil || resp.NextPage == 0 {
			return nil, nil
		}
		page = resp.NextPage
	}
}

// Create creates the GitLab Application and returns the secret as a connection detail.
func (e *external) Create(ctx context.Context, mg resource.Managed) (managed.ExternalCreation, error) {
	cr, ok := mg.(*v1alpha1.Application)
	if !ok {
		return managed.ExternalCreation{}, errors.New(errNotApplication)
	}

	if cr.Spec.WriteConnectionSecretToReference == nil {
		return managed.ExternalCreation{}, errors.New(errNoConnectionRef)
	}

	cr.Status.SetConditions(v2.Creating())

	app, _, err := e.client.CreateApplication(
		instance.GenerateCreateApplicationOptions(&cr.Spec.ForProvider),
		gitlab.WithContext(ctx),
	)
	if err != nil {
		return managed.ExternalCreation{}, errors.Wrap(err, errCreateFailed)
	}

	meta.SetExternalName(cr, strconv.FormatInt(app.ID, 10))
	cr.Status.AtProvider = instance.GenerateApplicationObservation(app)

	if cr.Spec.ForProvider.RenewalPeriodDays != nil {
		cr.Status.AtProvider.NextRenewalAt = &metav1.Time{Time: instance.NextRenewalTime(*cr.Spec.ForProvider.RenewalPeriodDays)}
	}

	return managed.ExternalCreation{
		ConnectionDetails: managed.ConnectionDetails{
			"secret":         []byte(app.Secret),
			"application_id": []byte(app.ApplicationID),
		},
	}, nil
}

// Update renews the OAuth client secret when a renewal period is configured and
// due. GitLab Applications have no general update endpoint, so any other
// up-to-date discrepancy (e.g. field drift) returns an error.
func (e *external) Update(ctx context.Context, mg resource.Managed) (managed.ExternalUpdate, error) {
	cr, ok := mg.(*v1alpha1.Application)
	if !ok {
		return managed.ExternalUpdate{}, errors.New(errNotApplication)
	}

	if !instance.IsApplicationRenewalDue(&cr.Spec.ForProvider, cr.Status.AtProvider.NextRenewalAt) {
		return managed.ExternalUpdate{}, errors.New(errCannotUpdate)
	}

	externalName := meta.GetExternalName(cr)
	appID, err := strconv.ParseInt(externalName, 10, 64)
	if err != nil {
		return managed.ExternalUpdate{}, errors.New(errIDNotInt)
	}

	app, _, err := e.client.RenewApplicationSecret(appID, gitlab.WithContext(ctx))
	if err != nil {
		return managed.ExternalUpdate{}, errors.Wrap(err, errRenewFailed)
	}

	cr.Status.AtProvider.NextRenewalAt = &metav1.Time{Time: instance.NextRenewalTime(*cr.Spec.ForProvider.RenewalPeriodDays)}

	return managed.ExternalUpdate{
		ConnectionDetails: managed.ConnectionDetails{
			"secret":         []byte(app.Secret),
			"application_id": []byte(app.ApplicationID),
		},
	}, nil
}

// Delete removes the GitLab Application.
func (e *external) Delete(ctx context.Context, mg resource.Managed) (managed.ExternalDelete, error) {
	cr, ok := mg.(*v1alpha1.Application)
	if !ok {
		return managed.ExternalDelete{}, errors.New(errNotApplication)
	}

	externalName := meta.GetExternalName(cr)
	if externalName == "" {
		return managed.ExternalDelete{}, nil
	}

	appID, err := strconv.ParseInt(externalName, 10, 64)
	if err != nil {
		return managed.ExternalDelete{}, errors.New(errIDNotInt)
	}

	cr.Status.SetConditions(v2.Deleting())

	_, err = e.client.DeleteApplication(appID, gitlab.WithContext(ctx))
	if err != nil {
		return managed.ExternalDelete{}, errors.Wrap(err, errDeleteFailed)
	}

	return managed.ExternalDelete{}, nil
}

// Disconnect is a no-op required by the SDK interface.
func (e *external) Disconnect(_ context.Context) error {
	return nil
}
